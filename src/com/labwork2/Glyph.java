package com.labwork2;

// Glyph objects have coordinate attributes and
// are used as components when building composite string objects.

public interface Glyph {
    int getX();
    int getY();
    char getChar();
}
